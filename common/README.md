# @thesteps/common

Framework-free domain model shared by all thesteps.to apps.

- `Plan.js`: plan/step model, validation and progress computation.
- Plans themselves are data (JSON files), not code; see `client/public/plans/`.

Tests use the Node.js built-in test runner (`npm test`).
