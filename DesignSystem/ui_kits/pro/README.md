# UI kit · /pro (providers)

Businesslike, denser. A vetted-lead inbox with a detail panel that shows **only the
data the user consented to share** (`.ts-shared` shared vs protected) and the
commission/billing model (paid by thesteps, never by the client). `index.html` +
`pro.css`. Same DS tokens, more density. Recreated from the brand brief.
